version: '2'
services:
  app:
    #container_name: voivi_app
    build: .
    #command: python manage.py runserver 0.0.0.0:8000
    ports:
    - "10101:10101"
    #volumes:
    # - .:/code
    #links:
    #- db
    depends_on:
    - db
  db:
    #container_name: voivi_database
    image: library/mongo:3.2.8
    #command: mongod -f mongod.cfg
    #volumes:
    #- ./data/db:/data/db
    ports:
    - "27017:27017"
    restart: always

#build:
#  context: ./dir
#  dockerfile: Dockerfile-alternate
#  args:
#    buildno: 1
